#!/usr/bin/python3
"""Installed entry point for one R9 panel lease endpoint or local command."""
from pathlib import Path
import sys

source_tree = Path(__file__).resolve().parents[1]
if (source_tree / "multimachine/display_panel_endpoint.py").is_file():
    sys.path.insert(0, str(source_tree))

from multimachine.display_panel_endpoint import main

raise SystemExit(main())
