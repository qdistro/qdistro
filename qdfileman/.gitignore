# Python
__pycache__/
*.py[cod]
*.pyo
*.pyc
*$py.class
*.so
.Python
.pytest_cache/
.hypothesis/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/
env/
ENV/

# Build & packaging (setuptools, wheel, etc.)
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
*.whl

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor & IDE
.vscode/
.idea/
*.swp
*.swo
*~
.cache/
.zed

# Configuration
!pyproject.toml

# Local git worktrees (the /do workflow puts them here). Never part of a
# commit — they are separate checkouts, so `git add -A` from the repo root
# would otherwise stage them as embedded git repositories.
.worktrees/
